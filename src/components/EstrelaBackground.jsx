import { useState } from "react"

//id, tamanho, x, y, opacidade, duracaoAnimacao

export const EstrelaBackground = () => {
    const [estrelas, setEstrelas] = useState([]);

    const gerarEstrelas = () => {
        
    }
    return <div> <div/>;
};